# Goal: Define all tools that Gemini can call during a conversation turn.
# Each tool maps to a backend action (set reminder, look up contact, query memory, etc.)
# and is registered in the format the Gemini function-calling API expects.
