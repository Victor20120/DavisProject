# Goal: Repository for memory_chunks with vector embeddings.
# Stores chunked text with embeddings and performs semantic similarity search
# so the orchestrator can retrieve the most relevant memories for a given query.
