# Goal: Memory extractor — runs asynchronously after each LLM response.
# Uses Gemini to identify new facts, relationships, and episode summaries
# from the latest turn and persists them to Firestore and the vector store.
